import { baseApiUrl } from '../../../../env'


export const movies = baseApiUrl + 'movie';
export const member = baseApiUrl + 'member';
export const memberActive = baseApiUrl + 'member-active';
export const lending = baseApiUrl + 'lending';